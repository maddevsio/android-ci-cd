json_key_file("key_firebase.json") # Path to the json secret file - Follow https://docs.fastlane.tools/actions/supply/#setup to get one
package_name(ENV["APP_PACKAGE_NAME"])
for_lane :firebase_distribution_staging do # [package name for staging]
  package_name(ENV["APP_PACKAGE_NAME_STAGING"])
end
